'use babel';

import LibreCopilotAtomView from '../lib/libre-copilot-atom-view';

describe('LibreCopilotAtomView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
